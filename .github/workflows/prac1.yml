name: Node.js CI
on:
  workflow_dispatch:
    inputs:
      logLevel:
        description: 'Log level'     
        required: true
        default: 'warning'
      tags:
        description: 'Test scenario tags'

jobs:
  build:
    runs-on: self-hosted

    steps:
      - name: Echo Test
        run: |
          echo "This Triggered" 
